# train and val data as 1) directory: path/images/, 2) file: path/images.txt, or 3) list: [path1/images/, path2/images/]
train: ../siam_cone_data/images/train/  # 128 images
val: ../siam_cone_data/images/val/  # 128 images

# number of classes
nc: 2

# class names
names: ['conenormal','coneabnormal']
